
##  webpack recode and study
```
  细节记录
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack"
  },
   npm run build -- --colors ==>  webpack "--colors"
```
